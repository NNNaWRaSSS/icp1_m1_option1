#define PI 3.14159

// Function prototypes
double rectangle_perimeter(double length, double width);
double rectangle_area(double length, double width);
double circle_perimeter(double radius);
double circle_area(double radius);
double triangle_perimeter(double side1, double side2, double side3);
double triangle_area(double base, double height);

//Fill in with the correct formulas to calculate the perimeter of a rectangle
//Return -1 if the input is invalid
double rectangle_perimeter(double length, double width) {
    return -1;
}

//Fill in with the correct formulas to calculate the area of a rectangle
//Return -1 if the input is invalid
double rectangle_area(double length, double width) {
    return -1;
}

//Fill in with the correct formulas to calculate the perimeter of a circle
//Return -1 if the input is invalid
double circle_perimeter(double radius) {
    return -1;
}

//Fill in with the correct formulas to calculate the area of a circle
//Return -1 if the input is invalid
double circle_area(double radius) {
    return -1;
}

//Fill in with the correct formulas to calculate the perimeter of a triangle
//Return -1 if the input is invalid
double triangle_perimeter(double side1, double side2, double side3) {
    return -1;
}

//Fill in with the correct formulas to calculate the area of a triangle
//Return -1 if the input is invalid
double triangle_area(double base, double height) {
    return -1;
}

